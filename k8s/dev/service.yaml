apiVersion: v1
kind: Service

metadata:
  name: sample-app-dev
  namespace: dev  

spec:
  selector:
    app: sample-app-dev

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 31011

  type: NodePort
